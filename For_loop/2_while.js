// While LOOp

    let index =0;
    while (index <= 10) {
        //console.log(`value of index is ${index}`);
        index++
    }

let arr = ['flash','batman','superman']
let int = 0
    while (int < arr.length) {
       // console.log(`the heroes are: ${arr[int]}`);
        int++
    }

// Do while Loop

let score = 1
    do{
         //console.log(`score is ${score}`);
         score++
    }while (score <= 10);

let myarray = ['vivek','bhardwaj']
let arr1 = 0;
do{
    //console.log(`the name is: ${myarray[arr1]}`)
    arr1++
}while(arr1 < myarray.length)